import { createTheme } from "@mui/material/styles";

const theme = createTheme({
    palette: {
      primary: {
        main: '#E85A4F',
      },
      secondary: {
        main: '#E85A4F',
      },
    },
    typography: {
        fontFamily: "Philosopher",
    },
    readmorereadless: {
        fontFamily: "Philosopher",
    },
  });

export {theme}